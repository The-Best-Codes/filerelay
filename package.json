{
  "name": "filerelay",
  "version": "1.0.4",
  "scripts": {
    "install": "cd frontend && bun install --frozen-lockfile && cd .. && cd backend && bun install --frozen-lockfile && cd ..",
    "build": "cd frontend && bun run build && cd .. ",
    "start": "cd backend && bun run start && cd .."
  },
  "author": "The-Best-codes",
  "cacheDirectories": [
    "frontend/node_modules",
    "backend/node_modules"
  ]
}
