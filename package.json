{
  "name": "filerelay",
  "version": "1.1.1",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "bun run --filter filerelay-frontend build",
    "start": "bun run --filter filerelay-backend start",
    "dev": "bun run --filter filerelay-frontend dev"
  },
  "author": "The-Best-codes",
  "trustedDependencies": [
    "better-sqlite3",
    "esbuild",
    "@tailwindcss/oxide"
  ]
}
